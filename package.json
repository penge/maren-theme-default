{
  "name": "maren-theme-default",
  "version": "1.1.1",
  "dependenies": {
    "pug": "2.0.4"
  },
  "scripts": {
    "install": "npm run css && npm run js",
    "css": "stylus -c < styles.styl > styles.min.css",
    "js": "uglifyjs scripts.js -o scripts.min.js -c -m --source-map"
  },
  "devDependencies": {
    "stylus": "^0.54.7",
    "toc-to-html": "^0.2.1",
    "uglifyjs": "^2.4.11"
  }
}
